import { WeightFormatPipe } from './weight-format.pipe';

describe('WeightFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new WeightFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
